# Copier ce fichier en .env et remplacer les valeurs.
# Générer un secret : python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-me
DATABASE_URL=sqlite:///./data/messagerie.db
JWT_EXPIRE_MINUTES=60
